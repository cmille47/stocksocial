const StockInfo = ({ stockData, position }) => {

    // all fundamentals
    // position conditional to show
    // current value/number of shares owned 

    return (
        <div>

        </div>
    );
};

export default StockInfo;